
services:
  nats-servers:
    container_name: nats-servers
    image: nats
    ports:
      - "4222:4222"
      - "8222:8222"
    restart: always
  
  # order-db:
  #   container_name: orders_database
  #   image: postgres:16.2
  #   restart: always
  #   volumes:
  #     - ./orders-ms/postgres:/var/lib/postgresql/data
  #   ports:
  #     - 5432:5432
  #   environment:
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=123456
  #     - POSTGRES_DB=ordersdb

  client-gateway:
    container_name: client-gateway
    depends_on:
      - nats-servers
    build: ./client-gateway #proceso de contruccion de la imagen esta en el client-gateway
    ports:
      - "${CLIENT_GATEWAY_PORT}:3000"
    volumes:
      - ./client-gateway/src:/usr/src/app/src
    command: npm run start:dev
    environment:
      - PORT=3000
      - NATS_SERVERS=nats://nats-servers:4222
  
  products-ms:
    container_name: products-ms
    depends_on:
      - nats-servers
    build: ./products-ms
    ports:
      - "3001:3001"
    volumes:
      - ./products-ms/src:/usr/src/app/src
    command: npm run start:dev
    environment:
      - PORT=3001
      - NATS_SERVERS=nats://nats-servers:4222
      - DATABASE_URL=file:./dev.db

  # orders-ms:
  #   container_name: orders-ms
  #   depends_on:
  #     - nats-servers
  #     - order-db
  #   build: ./orders-ms
  #   ports:
  #     - "3002:3002"
  #   volumes:
  #     - ./orders-ms/src:/usr/src/app/src
  #   command: npm run start:dev
  #   environment:
  #     - PORT=3002
  #     - NATS_SERVERS=nats://nats-servers:4222
  #     - DATABASE_URL=postgresql://postgres:123456@orders_database:5432/ordersdb?schema=public

 

  


